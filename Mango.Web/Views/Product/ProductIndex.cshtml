@model IEnumerable<Mango.Web.Models.ProductDto>

<br /><br />
<div class="container p-4 border">
    <div class="row pb-2">
        <div class="col">
            <h1 class="text-info">Products list</h1>
        </div>
        <div class="col text-end">
            <a asp-action="ProductCreate" class="btn btn-primary"><i class="bi bi-plus-circle"></i>&nbsp; New product</a>
        </div>
    </div>
    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Category</th>
                <th>Price</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach(var item in Model)
            {
                <tr>
                    <td>@item.Name</td>
                    <td>@item.CategoryName</td>
                    <td>@((item.Price).ToString("c"))</td>
                    <td>
                        <a asp-action="ProductEdit" class="btn btn-primary btn-success" asp-route-productId="@item.ProductId">
                            <i class="bi bi-pen"></i>
                        </a>
                        <a asp-action="ProductDelete" class="btn btn-primary btn-danger" asp-route-productId="@item.ProductId">
                            <i class="bi bi-trash"></i>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>